<h1>非表示タスク一覧</h1>
<% @hide_tasks.each do |hide_task| %>
  <div>
    <ul>
      <li>
        <%= link_to hide_task.title, hide_task %>
        <%= hide_task.created_at.strftime('%Y年%m月%d日') %>
      </li>
    </ul>
  </div>
<% end %>
<%= link_to "一覧", tasks_path %>